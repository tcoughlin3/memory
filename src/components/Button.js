import styled from 'styled-components';

export const Button = styled.button`
  border: 0;
  border-radius: 2px;
  font: inherit;
  outline: 0;
  padding: 4px 16px;
  margin-left: 12px;
`;

export default Button;
